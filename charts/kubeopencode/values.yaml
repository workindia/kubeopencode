# Default values for kubeopencode
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Controller configuration
controller:
  # Container image for kubeopencode (unified image with controller, git-init)
  image:
    repository: ghcr.io/kubeopencode/kubeopencode
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""

  # Number of controller replicas (only 1 active with leader election)
  replicas: 1

  # Resource limits and requests
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi

  # Service account for the controller
  serviceAccount:
    # Annotations to add to the service account
    annotations: {}
    # The name of the service account to use.
    # If not set and create is true, a name is generated using the fullname template
    name: ""

  # Security context for controller pods
  podSecurityContext:
    runAsNonRoot: true
    # runAsUser: 65532

  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
      - ALL

  # Node selector for controller pods
  nodeSelector: {}

  # Tolerations for controller pods
  tolerations: []

  # Affinity for controller pods
  affinity: {}

# Agent configuration
# NOTE: Agent ServiceAccount is NOT created by this chart.
# Users must create their own ServiceAccount and RBAC in each namespace where tasks run,
# and reference it in Agent.spec.serviceAccountName (required field).
# NOTE: Agent images are configured in Agent CRDs, not in this Helm chart.
# The two-container pattern uses:
#   - agentImage: OpenCode init container (default: ghcr.io/kubeopencode/kubeopencode-agent-opencode)
#   - executorImage: Worker container (default: ghcr.io/kubeopencode/kubeopencode-agent-devbox)
agent:
  # Default image settings (used for local development with Kind)
  image:
    pullPolicy: IfNotPresent

# Namespace configuration
namespaceOverride: ""

# Image pull secrets
imagePullSecrets: []

# Additional labels for all resources
commonLabels: {}

# Additional annotations for all resources
commonAnnotations: {}

# Server configuration (UI + API)
server:
  # Whether to enable the UI server
  enabled: true

  # Container image for the server (uses same image as controller)
  image:
    repository: ghcr.io/kubeopencode/kubeopencode
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""

  # Number of server replicas
  replicas: 1

  # Service configuration
  service:
    type: ClusterIP
    port: 2746

  # Authentication configuration
  # Enable when exposing the server externally (via Ingress or HTTPRoute) for agent proxy access.
  # Uses Kubernetes TokenReview to validate Bearer tokens from kubeconfig.
  auth:
    enabled: true
    # Allow unauthenticated requests (for development only)
    allowAnonymous: false

  # Ingress configuration
  ingress:
    enabled: false
    className: ""
    # The agent proxy uses SSE (Server-Sent Events) for streaming. Depending on your
    # gateway controller, you may need to add annotations it to disable response buffering
    # and increase timeouts.
    annotations: {}
    hosts:
      - host: kubeopencode.local
        paths:
          - path: /
            pathType: Prefix
    tls: []
    #  - secretName: kubeopencode-tls
    #    hosts:
    #      - kubeopencode.local

  ## route (map) allows configuration of HTTPRoute resources
  ## Requires Gateway API resources and suitable controller installed within the cluster
  ## Ref. https://gateway-api.sigs.k8s.io/guides/http-routing/
  ##
  ## The agent proxy uses SSE (Server-Sent Events) for streaming. Depending on your
  ## gateway controller, you may need to configure it to disable response buffering and
  ## increase timeouts.
  route:
    main:
      ## Enable this route
      enabled: false

      ## ApiVersion set by default to "gateway.networking.k8s.io/v1"
      apiVersion: ""
      ## kind set by default to HTTPRoute
      kind: ""

      ## Annotations to attach to the HTTPRoute resource
      annotations: {}
      ## Labels to attach to the HTTPRoute resource
      labels: {}

      ## ParentRefs refers to resources this HTTPRoute is to be attached to (Gateways)
      parentRefs: []
        # - name: contour
        #   sectionName: http

      ## Hostnames (templated) defines a set of hostnames that should match against
      ## the HTTP Host header to select a HTTPRoute used to process the request
      hostnames: []
        # - my.example.com

      ## additionalRules (templated) allows adding custom rules to the route
      additionalRules: []

      ## Filters define the filters that are applied to requests that match
      ## this rule
      filters: []

      ## Matches define conditions used for matching the rule against incoming
      ## HTTP requests
      matches:
        - path:
            type: PathPrefix
            value: /

      ## httpsRedirect adds a filter for redirecting to https (HTTP 301 Moved Permanently).
      ## To redirect HTTP traffic to HTTPS, you need to have a Gateway with both HTTP and HTTPS listeners.
      ## Matches and filters do not take effect if enabled.
      ## Ref. https://gateway-api.sigs.k8s.io/guides/http-redirect-rewrite/
      httpsRedirect: false

  # Resource limits and requests
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi

  # Service account for the server
  serviceAccount:
    # Annotations to add to the service account
    annotations: {}
    # The name of the service account to use.
    # If not set and create is true, a name is generated using the fullname template
    name: ""

  # Security context for server pods
  podSecurityContext:
    runAsNonRoot: true

  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
      - ALL

  # Node selector for server pods
  nodeSelector: {}

  # Tolerations for server pods
  tolerations: []

  # Affinity for server pods
  affinity: {}

  # Port-forward RBAC configuration
  # Creates a Role/RoleBinding to allow specific subjects to port-forward
  # the UI server (kubectl port-forward svc/kubeopencode-server).
  portForward:
    enabled: false
    # Subjects to grant port-forward access to
    # Example:
    #   subjects:
    #     - kind: ServiceAccount
    #       name: my-sa
    #       namespace: my-namespace
    #     - kind: User
    #       name: developer@example.com
    subjects: []

# KubeOpenCodeConfig - system-level configuration
# This creates a default KubeOpenCodeConfig in the release namespace
kubeopencodeConfig:
  # Whether to create the default KubeOpenCodeConfig
  create: true
  # Cluster domain name (e.g., "cluster.local")
  # This is used for constructing in-cluster service URLs.
  # If not specified, "cluster.local" is used as the default.
  clusterDomain: ""
  # System image configuration for internal components (git-init, context-init)
  systemImage:
    # Image to use (empty = use controller image)
    image: ""
    # Image pull policy
    imagePullPolicy: IfNotPresent
  # Cleanup configures automatic deletion of completed/failed Tasks.
  # Rendered into KubeOpenCodeConfig.spec.cleanup. Both fields are optional and
  # independent; when both are unset the cleanup block is omitted entirely, so
  # existing installs are unaffected.
  cleanup:
    # Delete completed/failed Tasks this many seconds after CompletionTime.
    # Unset = TTL-based cleanup disabled. Note: 0 is a valid value (delete
    # immediately once finished).
    ttlSecondsAfterFinished: null
    # Keep at most this many completed/failed Tasks per namespace, deleting the
    # oldest first. Cluster-wide: the same limit applies to every namespace.
    # Unset = retention-based cleanup disabled.
    maxRetainedTasks: null
  # OpenTelemetry observability configuration
  # When enabled, the controller injects OTLP environment variables into agent Pods
  # so that OpenCode's built-in OTel support is activated automatically.
  # KubeOpenCode produces OTLP data; the user's Collector collects, processes, and stores it.
  observability:
    openTelemetry:
      # Enable OpenTelemetry telemetry for agent Pods
      enabled: false
      # OTLP/HTTP endpoint for the user's OpenTelemetry Collector.
      # Required when enabled is true.
      # Examples:
      #   - Gateway mode:  http://otel-collector.observability:4318
      #   - Sidecar mode:  http://localhost:4318
      #   - External SaaS: https://api.honeycomb.io
      # Note: OpenCode uses OTLP/HTTP (port 4318), not gRPC (port 4317).
      endpoint: ""
      # Optional headers for collector authentication (e.g., SaaS API keys).
      # Values can be inline or resolved from a Secret via valueFrom.secretKeyRef.
      headers: {}
      # Inject experimental.openTelemetry into OpenCode config to enable LLM call traces
      # with GenAI semantic conventions (model, tokens, latency).
      enableLLMTraces: false
      # Whether to record full prompt/response content on LLM spans.
      # Default false; set true only in trusted environments.
      recordContent: false
      # Additional resource attributes for all spans.
      # The controller also automatically injects: kubeopencode.task.name,
      # kubeopencode.task.namespace, kubeopencode.agent.name, k8s.namespace.name, k8s.pod.name.
      resourceAttributes: {}
